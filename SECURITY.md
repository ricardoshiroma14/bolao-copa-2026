# Security Policy

Do not commit secrets to this repository.

Use `.env.example` for placeholders and keep real values in your local or hosting-provider secret store. If a service role key, OAuth secret, or API key is ever committed, rotate it immediately in the provider dashboard.

For vulnerability reports, open a private security advisory or contact the repository owner.
